{
  "name": "@monorepo/root",
  "version": "1.0.0",
  "main": "packages/",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev --parallel --no-daemon",
    "build": "turbo run build --no-daemon"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "turbo": "1.9.1"
  }
}
