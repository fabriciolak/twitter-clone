{
  "name": "@monorepo/root",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev --parallel --no-daemon",
    "build": "turbo run build --no-daemon"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "1.9.1"
  }
}
